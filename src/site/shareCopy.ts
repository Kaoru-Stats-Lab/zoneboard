/** Paste into YouTube / Discord / Twitch — awareness without a live pitch watermark. */
export const STREAM_SHARE_BLURB =
  "Board: https://zoneboard.app — no account. Press B to hide tools. Window capture in OBS.";

/** Drawn in the PNG footer only when bakeCredit is on (never on live capture). */
export const PNG_CREDIT_TEXT = "zoneboard.app";
