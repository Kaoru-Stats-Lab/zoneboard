# ZoneBoard — プロダクトメモ

**更新:** 2026-07-04  
**リポジトリ:** `C:\asl_dev\zoneboard`  
**ステータス:** 方針メモ済 · **仕様（SPEC）ドラフト承認待ち（市場調査反映済）** · 実装未着手  
**SUGUDASU台帳ポインタ:** `C:\asl_dev\sugudasu\docs\notes\PRODUCT_IDEA_JUDGMENT_LEDGER.md` §16  
**ブランド:** **ZoneBoard**  
**ドメイン:** **`zoneboard.app`**（本拠）。`zoneboard.com` はプレミアム約63.6万円 → **不取得**

---

## 0. 動機（提督）

ワールドカップ解説など YouTube で、戦術説明を **Googleスライドの図形いじり**でやっているのを見た。サッカー・バレー・バスケなど **俯瞰して戦術を指示する**用途なら長時間使う。Privnote型（浅いPV）と単位経済が違う。

**参考UI:** TacticsLista 等の既存Web戦術ボード（ピッチ俯瞰・選手円・フォーメーション・描画）。機能の方向性は近いが、**配信時にピッチが小さすぎる**のが不満。運営ロゴ透かしではなく **自分のロゴ**にしたい。

---

## 1. ジョブ

| 誰 | 今日の痛み |
|----|------------|
| 解説系 YouTuber / アナリスト | スライドでコート比・背番号・矢印が毎回崩れる。配信中は画面共有しっぱなし |
| **VTuber（同時視聴・W杯実況）** | 俯瞰ボードを画面の主役にし、アバター・スコア・チャットは OBS で重ねたい。運営ロゴではなく **自分の色**（ロゴ・チーム色・名前付き駒） |
| 部活・アマ監督 | ホワイトボードか紙。共有は写真か口頭 |
| 多競技 | コートだけ違う。駒・矢印・ゾーン・手順番号の型は同じ。**v1 はサカ/バス/バレに固定**（[`SPORTS_SCOPE.md`](SPORTS_SCOPE.md)） |

### VTuber 同時視聴で「ここまでやりたい」境界

参考: W杯同時視聴配信（俯瞰ボード全面＋アバター隅＋スコア帯＋チャット）。

| ZoneBoard が担う | OBS / 配信ソフトが担う（やらない） |
|------------------|-------------------------------------|
| 全画面に近いピッチ（配信モード） | VTuber アバター・立ち絵 |
| 両チーム色・背番号・名前（任意）の駒 | スコアボード・タイマー・テロップ |
| ユーザロゴ透かし | スーパーチャット・アンケート UI |
| スタメン一括配置・局面・ズーム | チャット欄・同時視聴の映像ソース |
| PNG 書き出し（縦投稿用） | 配信エンコードそのもの |

**独自色の出し方:** ロゴ・チーム色・名前表示・（Later）コート色。アバター埋め込みやスコア機能は持たない。

ジョブは「保存場所」ではなく **空間＋駒＋動きの編集器**。

**学習コスト指針:** 「使いこなしたい人が最小学習コストでいける」を優先する。ホットキー・自由曲線・局面切替・画角ズームは **任意の加速装置**。使えない／使わない人は **駒を動かすだけ**で戦術を表現できる、と割り切る。初心者向けウィザードや機能制限で上級者を遅くしない。

**UI/UX 正本（chrome・設定・パネル）:** [`UI_UX.md`](UI_UX.md) — 配信者（デジタルネイティブ）の慣れ（OBS / Canva / Figma）に寄せる。戦術管理画面にしない。

**フットサル・ビーチ:** ジョブ／ペルソナ／配信境界のリサーチは [`FUTSAL_BEACH_RESEARCH.md`](FUTSAL_BEACH_RESEARCH.md)（サッカーと同粒度。現場 FB 待ち）。

**タッチ:** タブレット・大型タッチパネルを想定。1本指で駒／描画、ピンチズーム、2本指パン。ホットキーは PC 限定。物理マグネットボードと同じ「指で動かす」体験をデジタルで再現する。

**ボールと駒:** マルチ選択は使わない。ボールを駒に重ねて離すと吸着し、その駒を動かすとボールも動く（保持者のイメージ）。ボールだけドラッグすれば解除。

**背番号ファースト:** 高校選手権の地方大会やブラジル州選手権など、別地域の配信者は **名前が分からず背番号だけ分かる** ことが多い。名簿・スタメンは番号必須・名前任意。駒の識別子は背番号。

**利き足（任意）:** VTuber・解説向け。駒に L/R/B を小さく表示（向き基準の足側）。未設定なら非表示。バスケ／バレでは使わなくてよい。

**セッション特性:** 1本の解説・1試合準備で **30分〜数時間**。再訪・テンプレ再利用あり → 広告/Pro の単位経済は Privnote よりマシ。

---

## 2. UX 正本 — 配信で使える領域を最大化

既存ツールの典型問題:

- ツールバーが縦に2〜3段、画面の **25〜30%** を食う
- ピッチ周囲にデッドスペース
- 配信では **ピッチと駒・矢印だけが大きく見えてほしい**

### 2-1. 必須方針

| 方針 | 内容 |
|------|------|
| **Canvas first** | コート／ピッチがビューポートの大半（目安 **≥80%**） |
| **Chrome 最小化** | 設定・保存・フォーメは折りたたみパネル。配信中は閉じる |
| **配信モード** | ツールバー非表示 or 極小、余白ゼロ、ピッチ最大表示 |
| **ホットキー** | 描画・UNDO・駒はキーボード中心 |
| **余白を殺す** | 装飾ネイビー余白を配信モードでは出さない |

### 2-2. 配信モードで残してよい最小UI

- 現在ツールの小さなインジケータ
- UNDO（または Ctrl+Z のみ）
- 「配信モード解除」だけ端に小さく
- **ユーザー設定のロゴ透かし**（§2-4）— ツールバーを隠しても残す

保存・Export・フォーメ・透かし設定は配信モード外 or Esc で閉じるオーバーレイ。

### 2-3. 書き出し

- 透過 PNG（ロゴ透かし焼き込み ON/OFF）
- 手順コマ連番は v1.1

### 2-4. ロゴ透かし（YouTuber カスタム）

| 項目 | 方針 |
|------|------|
| **入力** | ローカル画像（PNG・透過可）。コア方針なら非送信 |
| **表示** | 半透明。四隅プリセット。サイズ・不透明度調整。**贔屓クラブ／協会ロゴはここに置く（芝の上）** |
| **配信モード** | 透かしは常時表示 |
| **タイトル帯** | 試合名バナーは透かしと別・テキスト編集可。スコアは得点から。**国・クラブの紋章は帯に入れない**（SEA 実況のスコアバグ型は真似しない） |
| **やらない** | 運営強制ウォーターマーク。Webcam をボードに埋め込まない（OBS側）。スコア帯へのクレスト埋め込み |

---

## 3. 最小機能（v1 仮説）

1. 競技プリセット（サッカー / バスケ / バレー、寸法比正しい）
2. 駒（背番号・名前・色・フォーメプリセット）
3. 描画（矢印・線・ゾーン・フリーハンド・テキスト）
   - ゾーン＝空き（◯）。**ブロックを敷くは Pen**（横線2本／囲む）。ブロック専用ツールは持たない
   - Pen は解説インク。液タブ級の筆は追わない。滑らかさは合算点＋ベジェまで（B-047）
4. 試合タイトル帯
5. ロゴ透かし（v1 必須）
6. ローカル保存（JSON / localStorage）
7. 配信モード（v1 必須）
8. 手順再生は v1.1

**やらない（v1）:** 選手DB、**予想スタメン専用機能**（名簿＋配置で足りる。独自色はロゴ・解説側）、**試合全体の動画解析・連続トラッキング**、スクレイピング、SNS投稿、重いアニメ、ボード内Webcam、運営ロゴ強制。**サカ/バス/バレ以外の競技**（ラグビー・アメフト・野球・eSports 等は Later）。**運営カタログのフォーメプリセット**（4-4-2 等を商品として並べない。下記決定ログ）。**視野角の扇形**（概念は知っている。出さない。下記決定ログ）。

※ **局面取込（Broadcast Capture Import）** は上記「動画解析」とは別。1枚／1フレーム → scene ドラフトは Later 大型候補（下記決定ログ · B-070）。

### 決定ログ — Broadcast Capture Import / 局面取込（2026-08-30）

**次の大型アップデートの芯: 配信メディア → 局面ドラフト。Deep Research により Go。**

根拠: [`BROADCAST_CAPTURE_IMPORT_RESEARCH.md`](BROADCAST_CAPTURE_IMPORT_RESEARCH.md)。

| 項目 | 方針 |
|------|------|
| **英名 / 日名** | Broadcast Capture Import / **局面取込** |
| **Verdict** | **Go**（Later 大型 · Phase 付き）。連続トラッキングは対象外 |
| **ジョブ（本線）** | **ロースコア球技ではライブ中に何度も取込→配置→解説は稀。** ボールが動き続けるので、解説中にシーンが変わる。本線は **DAZN / スポーツTV / Abema / リーグ公式動画などでシーク → 複数キャプチャ → 自動配置＋手修正 → ハーフタイム（または停止区間）で解説** |
| **ジョブ（副）** | ライブ中の即時取込は **ボーナス**（セットプレー静止・VAR待ち等）。本線の成功条件にしない |
| **正本** | **ゴースト／下敷きはドラフト。確定前に上書きしない。手修正が本線。100%再現は目指さない** |
| **入力** | ① 動画シークからのフレーム（需要大） ② `Ctrl+V` スクショ。主アングルは **斜め実写** → Homography 要 |
| **キャリブ（決定）** | **4点はユーザが手動で置く**のが本線（Phase 1）。画質が悪いと線が見えず自動では無理。アングルも試合・局・ズームで毎回違う。自動ピッチ認識は Phase 3 研究のみ |
| **キャリブ対象ピッチ（決定）** | Homography は ZB の `soccer` 実寸（既定 **105×68 m**）が **暗黙の標準**。UI に「フルサイズ協会ピッチ向け」等は **デフォルトで書かない**（大仰・自明）。**ジュニア／縮小コートは現時点対象外** → 次期に **ジュニアコート**として明示追加するまでキャリブ拡張しない。同寸のプロ国内は当然含む。フットサル・ビーチは別競技ページ |
| **精度期待** | **入力で階梯を分ける。** 低画質 → たたき台＋手直し。**DAZN 級ではほぼ配置どおりを本音目標（ゴールド・上振れ）。** ユーザジョブは駒が多いほど **「だいたい置いてくれる」だけで助かる**。出荷の最低は密度が高い局面で下書きが効くこと |
| **画角の難易度（観察）** | **最近の DAZN は高所からの戦術俯瞰が主** → ピッチ線が読みやすく **キャリブしやすい（ゴールド向き）**。**昔の映像**は低め・寄り・画質劣化・4:3引き伸ばしが重なり **しんどい**。難度は画質 × 画角 ×（必要なら）アスペクト補正 |
| **縦ピッチ（Later · やる）** | **B-072。** 正本 [`VERTICAL_PITCH.md`](VERTICAL_PITCH.md)。描画は実装済寄り。**UI:** [The Tactics App](https://thetacticsapp.com/) 型 Pitch View（5線画アイコン）を採る。向き変更のデータ意味は未決 |
| **「見たまま」PNG（決定）** | 放送スクショ下地＋駒のコンポジット |
| **駒ラベル（制約）** | 背番号は画面に対して正立必須 |
| **名前ピル（B-073 · 実装済）** | `showPlayerNames` ボード単位。新規 OFF · migrate 欠落 true。ON＝芝上も暗チップ 1 種。フォーメ配信（VTuber 型）向け。[`AGENT_PROMPT_PLAYER_NAME_PILL.md`](AGENT_PROMPT_PLAYER_NAME_PILL.md) |
| **実写への赤入れのみ（決定 · 2026-08-30）** | **ZB に入れない。** 放送スクショ上に矢印／囲みだけ描いて PNG、は [SUGUDASU 赤入れ](https://sugudasu.com/annotate) の席。**赤入れでできないのは Run / Dribble / Pass（構造線＋駒）の表現。** ZB の楔はそこ。局面取込後の下地に構造線が載るのは大型（B-070）の付帯で、赤入れの移植ではない |
| **優先入力ソース（決定）** | **ゴールド検証の正本:** **DAZN** · **Abema（DAZN 配信）** · **SPOTV NOW（YouTube 上含む）** · **U-NEXT（YouTube 上）** |
| **汚い入力（決定）** | ブラウザ全体のゴチャつきスクショを想定。初期は **ピッチ ROI / 手動クロップ**。サイドバーサムネを誤検出しない |
| **アスペクト（決定）** | **ゴールドは正しいアスペクト**（現行 16:9 等）。4:3→16:9 引き伸ばしは当面対象外。**Later: 4:3 補正（アンストレッチ）**がアーカイブ需要で要る可能性が高い（昔の画像ほど） |
| **マルチフレーム（ゴールド）** | 同じ局面で数枚を取れるなら最良フレーム選定 · 足元安定化 · ポーズ UI 回避。キャリブは1回。ゴールド精度の本命手段 |
| **Phase 1** | 下敷き + **手動4点 Homography** + 手置き（または後続の自動駒）。自動4点当てに依存しない |
| **Phase 2** | クライアント **自動駒ドラフト** → ゴースト微調整 → 確定。キャリブは手動4点のまま。**身元は画像からは当てない**（下記ピック） |
| **Phase 2 付帯 · 身元ピック** | 検出は位置だけ。背番号 OCR・誰か同定は本線にしない。**ベンチ入りが分かっている試合**で、配信者がゴースト／仮駒を選び、**スカッド（またはその瞬間のピッチメンバー）から即ピック**して番号・名前を当てる |
| **Phase 3** | ピッチ線の完全自動キャリブ（研究 · 画質依存が強い） |
| **コマ抜き** | **表は ZB · エンジンはボード本体と分離**（共有モジュール or 同一ドメイン別面）。処理は **ブラウザ・ローカルメモリ**。外部 sugudasu リンク / iframe は非推奨。SUGUDASU ブランド非掲出 |
| **守る境界（ハード）** | OBS 窓キャプチャを途切れさせない。ローカル・ブラウザ。机は **Single PC · 2画面（watchalong）**。設計標的 **帯 B = RAM 16GB**（[`STREAMER_RIG_RESEARCH.md`](STREAMER_RIG_RESEARCH.md)）。帯 C（8GB）は自動抽出制限。Worker · プロキシ解像度 · 短命バッファ。サーバは最終手段 |
| **非送信** | デフォルト端末内。クラウド Vision / サーバ側トランスコードは採らない前提 |
| **やらない** | 試合全体解析 · 線自動 · 選手 DB · Piero 席 · 必須ウィザード |
| **調査** | プロンプト [`AGENT_PROMPT_BROADCAST_CAPTURE_IMPORT.md`](AGENT_PROMPT_BROADCAST_CAPTURE_IMPORT.md) |
| **Backlog** | **B-070** |
| **ロールアウト（2026-08-31）** | **Deploy 済みでも UI 非公開。** 本番 QA は `?captureImport=1` → `localStorage zoneboard:v1:captureImportBeta`。changelog / LP / How-to は **公開決定まで書かない**。実装: [`AGENT_PROMPT_CAPTURE_IMPORT.md`](AGENT_PROMPT_CAPTURE_IMPORT.md) §隠し機能 |

v1 コア（手置き · B · ローカル）を壊さない。出荷は Phase 1 から。コマ抜きの分離は **管理がスムーズなら決定**（モノリス必須ではない）。

**プレイヤー UI / 一時停止オーバーレイ（Phase 1–2 既知リスク）:**

| 対策の方向 | 内容 |
|------------|------|
| **予防** | コマ抜きエンジンで **一時停止せず前後フレームを抜く** / スクショ前に UI を隠す案内 |
| **クロップ** | 中央の巨大ポーズアイコン領域をマスク、またはピッチ ROI だけ渡す |
| **手修正** | 誤検出をゴーストから消す UX（Draft & Snap の一部） |
| **やらない（初期）** | 放送局ごとの UI 除去モデルを本線化（メンテ地獄） |

**優先配信ソース（2026-08-30 · スクショ確認）:**

ゴールドパスの検証・マスク設計は次を正とする（JP watchalong の机）。

| ソース | 典型クロム（画面上） | メモ |
|--------|----------------------|------|
| **DAZN** | 左上スコアバグ · **右上 DAZN ロゴ** · 下にプレイヤーバー | 高画質ゴールドの本命 |
| **Abema（DAZN）** | 同様に右上 DAZN · 左上試合帯 · 下に Abema 系コントロール／ハイライト導線 | DAZN 映像＋Abema シェル。バー・「ショートハイライト」等に注意 |
| **SPOTV NOW**（YouTube 上含む） | 右上 **SPOTV NOW** · 左上リーグ帯 · YouTube なら下に YT コントロール | 埋め込み／YT で下帯の厚みが違う |
| **U-NEXT**（YouTube 上） | 右上に **大きな U-NEXT 宣伝オーバーレイ**（ハッシュタグ・サッカーパック文言）· 映像内にも薄い U-NEXT 透かし · 下は YT コントロール | 宣伝ブロックが芝・スタンドを広く覆う。ROI／手動枠が特に効く |

共通: ピッチ線は読める。検出 ROI は **中央〜下寄りの芝**を優先し、四隅ロゴ・下コントロール・**宣伝オーバーレイ**は初期マスク候補。プレイヤー別の専用 ML は作らない（§ 既知リスクどおり）。レイアウト詳細: [`BROADCAST_CAPTURE_IMPORT_RESEARCH.md`](BROADCAST_CAPTURE_IMPORT_RESEARCH.md) §2d。

**身元ピック（Phase 2 付帯 · 2026-08-30）:**

画像処理では **背番号も個人も当てない**（OCR・選手 DB はやらない）。配置ドラフトのあと、解説者が「コイツだ」と当てる。

| 前提 | 内容 |
|------|------|
| **いつ使えるか** | その試合の **ベンチ入りメンバーが名簿に入っている**とき（今夜の roster）。空名簿ではピック先がない |
| **候補セット** | ① **マッチデイ・スカッド全体**（XI+ベンチ） ② **その局面でピッチにいたメンバー**（ユーザが絞る／既にピッチ上の駒と未使用の差集合） |
| **操作** | ゴーストまたは仮駒を選択 → 候補チップ／番号一覧から **即ピック**（キーボード番号打ちも可なら加速） |
| **なぜピックで足りるか** | 贔屓チームを配信する人は **立ち姿・背格好・走り方**で「誰か」が瞬時に分かる。画像 AI に同定させず、**人が見て選ぶ UI を最短にする**のが本線 |
| **識別子** | 既存どおり **背番号ファースト**（名前任意）。当てた結果は `piece` に番号・ラベルを載せる |
| **やらない** | Transfermarkt 同定 · 顔認識 · 背番号 OCR 必須化 · スカッド未登録試合での魔法 |

HT で「複数枚取込 → 配置 → **誰か当て** → 解説」が一本の流れ。詳細: [`BROADCAST_CAPTURE_IMPORT_RESEARCH.md`](BROADCAST_CAPTURE_IMPORT_RESEARCH.md) §1b · §4d。

### 決定ログ — 駒の向きと視野角扇（2026-08-25）

**視線・視界・体の向きは Football で大事。ZB は体の向き（`facing`）一本で足りるとし、視野角の扇形は出さない。**

知らないから載せないのではなく、戦術語として認識したうえで **敢えて出さない**。体向きと視線を別属性にすると学習コストが跳ね、扇は配信映えと誤読のリスクが高い。口頭解説・線・ゾーンで足りる。

- **持つ:** `facing`（三角ドラッグ・`R`・移動追従）。利き足 L/R もこの向き基準
- **出さない:** 視野角扇、視線と体向きの二属性、駒カード上の向き UI（カードは背番号・名前・利き足のみ）
- **発見性は別件:** 三角が装飾に見える問題は、扇を足さずにヒント／how-to で扱う

**利き足の見せ方（2026-08-31 · ログのみ · 未実装）:** 参考 UI の **コマ縁ノブ（文字なし）** はスッキリするが、初見の発見性・両利き・**向き三角との混同**が弱い。全面置換はしない。**向き三角は残す。** 利き足は向き基準の **L/R 小丸**を維持し、名前ピル ON 時はさらに控えめ（または片側ドットのみ）を Later で検討。データは `preferredFoot` のまま。

旧 SPEC の「扇 = v1.1 Should」は本決定で上書き。ロードマップに戻さない。

### 決定ログ — ズーム時の駒サイズと Match Piece Size（2026-08-25）

**CK 等で画角を寄せたとき、駒はピッチと一緒に膨らませず、相対的に小さくして密度を解く。ただし Match の Piece Size（`pieceScale`）との整合を崩さない。**

いまの半径はフルピッチ矩形（ズームで画面上に伸びる方）基準なので、ズーム＝駒も同じ倍率で大きくなる。タクティカルには逆。狭い範囲（CK・ペナ）に両軍を詰めたいときに粒度が死ぬ。近年の CK 重要度とも合う。

**二層モデル（必須）:**

| 層 | 何か | UI |
|----|------|-----|
| **意図** | `pieceScale`（戦術大 / 標準 / 精密小・スライダー） | Match の Piece Size。ユーザが決める「この試合の駒の大きさ」 |
| **密度補正** | ズームに応じて半径を抑える（画面ほぼ一定、または `/ zoom^k`） | 自動。Match の数値を書き換えない |

- **整合:** フル画角で「戦術」なら大きく、「精密」なら小さい。寄せたあともその大小関係は保つ。補正は乗算のみ
- **やらない:** ズームで `pieceScale` をサイレント変更、CK プリセットだけ精密に飛ばす（ホイールと不整合）、Piece Size UI を隠してズーム任せにする
- **表示:** Match の `(1.00)` 等は常に保存値。画面上の見かけ半径とはズーム時に一致しなくてよい（意図の表示）

実装済（`densityZoom`：画面サイズはフル画角相当、`pieceScale` は乗算のみ）。**Pen・Link も同補正。パス／ラン・ピッチ線はピッチ比例のまま** — 詳細は下記 **2026-08-28** 決定ログ。

### 決定ログ — フォーメプリセット（2026-08-25）

**ZoneBoard は運営プリセットのフォーメーションを持たない。**

フォーメは試合・局面・監督で融通無碍で、カタログ化できない。Match の「フォーメ＋サブ配置」は「いまの名簿を保った並べ直し」に読めず、内部の 1–11 / 12–23 足場でピッチを差し替える破壊操作だったため **UI から非表示**。

- **残す:** 新規ボード／競技切替などの空を埋める内部足場（`formationPieces`）。商品名としては出さない
- **出さない:** 4-4-2 等の運営フォーメ一覧、Match 上の再適用ボタン
- **Later（有料 Local Library）:** ユーザが自分で保存した配置・局面・名簿セットの呼び出し。運営カタログではない。クラウド盤面 SaaS ではない（§6）

本線は **名簿 → チップで XI → ピッチ**。形はユーザが駒を動かす。

### 決定ログ — チーム表示（Home / Away / Both）と hideHalf（2026-08-25）

**よくある解説:** フルピッチでホームの形 → アウェイの形 → 両軍を重ねてシミュレーション。欲しいのは **所属チームの表示フィルタ**（駒は消さない）。

**タイミング:** 実況のインプレー中ではなく、**試合前・試合後（準備・振り返り）**。配信 chrome や Broadcast ワンタップ本線にはしない。

**置き場所（方針）:** **Scenes（準備で触る面）**。局面の「いま何を見せるか」。Match／ツールレール／配信端は不向き。

**制作会社で色が出る:** 局面3枚に分ける／1局面でフィルタ切替／半面だけ見せる等は会社・番組で違う。ZB はフィルタを **任意の準備ツール** として持ち、運用を強制しない。

**`hideHalf`（左右の空間）:** 機能として残してよい。セットプレーは画角ズーム推奨（SPEC）。**使い所の正本は薄い**——左右カットが番組文法の制作向けレガシー。チーム表示フィルタと役割を混ぜない（空間 ≠ 所属）。

- **やる（実装済）:** Scenes に Home / Away / Both。描画フィルタのみ（`teamFocus`）
- **やらない:** 配信 chrome の常設切替、実況ホットキー本線化、hideHalf をチーム表示の代わりにする

実装済（B-055）。

### 決定ログ — Pass ＝ ボール軌道（ラベル認知）（2026-08-25）

**設計:** `pass` 線種 ＝ ボールの移動（パス・クロス・シュート含む）。Select には描画を入れない。

**ユーザ認知:** 英語 Native の「Pass」は味方へのパスに狭く、CK クロス／シュート軌道までは読み取りにくい。コーチングアプリ経験者は Pass ツール＝ ball movement と分かる層もいるが、解説者向け ZB では **ヒントで ball path と明示**する（B-058 済）。

**Later:** `shot` 線種で Pass と意味分離（B-059）。TacticsBoard / FTB 追随。立体山なり UI とは別論点 — [`LOFTED_BALL_PATH_RESEARCH.md`](LOFTED_BALL_PATH_RESEARCH.md)。

### 決定ログ — 山なり（浮き球・クロス）軌道は v1 入れない（2026-08-30）

**2D 真上視点では曲線の破線矢印が世界標準。3D 放物線専用 UI は v1 非対象。**

| 判断 | 内容 |
|------|------|
| **v1** | **やらない** — 現状のパス（破線 + 自由曲線 / `smoothLinePath`） |
| **Later** | 新線種・Arc トグルは入れない。Pass の **スムージング / 軽量カーブ微調整** のみ（B-061） |
| **不可** | 3D 疑似アーチ · `loft` 線種 · ツールレール肥大 |

根拠: [`LOFTED_BALL_PATH_RESEARCH.md`](LOFTED_BALL_PATH_RESEARCH.md)（Deep Research 2026-08-30）。配信楔（1 ストローク · B · 無学習）と OBS 圧縮視認性を優先。

### 決定ログ — Pen + Shift = 直線（2026-08-25）

**Pen 1本。Shift 押しながらドラッグ → 始点–終点の直線。** Figma 型。Bunseki 型の構造チェーン・チャネル境界に使う。第4線種・自動直線推測は作らない。Zone の Shift（正円）とは別ツールで衝突しない（B-060 済）。

### 決定ログ — ツールモード契約（2026-08-31 · **実装済 · 2026-08-31 改定②**）

**原則: 明示的にメニューを変えない限り、そのツールを維持。** レールで別ツールを選ぶ · **`V` で選択** · 配信メニュー切替 — 以外は自動で `select` に戻さない。

| 契約 | ツール | 1アクション後 |
|------|--------|----------------|
| **選択** | `select` | —（`V`） |
| **配置** | `piece-home` · `piece-away` | **そのまま**（連続配置） |
| **描画** | `pass` · `run` · `dribble` · `screen` · `pen` · `zone` | **そのまま**（連続描画） |
| **テキスト** | `text` | **入力確定（Enter / blur）で `select` に戻る** — 唯一の例外 |
| **リンク** | `link` | **そのまま**（鎖確定後もリンク · 次の鎖を続けられる） |
| **ボール** | `ball` | **そのまま** |

**例外 — テキストのみ:** ツールが `text` の状態で入力を **確定** したら `select` に戻す（ラベル1個置いて次は駒移動、が多い）。キャンセルだけでは戻さない。選択モードから既存テキストを直す場合は `select` のまま。

**リンク:** 鎖確定（同駒再クリック · 空き地 · Enter）後も **リンクのまま**。Esc は鎖だけ取消。

**発見性（編集モード）:** 現在ツール名常時表示 · 選択ピン · リンク active 強化 · リンク途中サブ状態 — スティッキー前提でより重要。

実装プロンプト: [`AGENT_PROMPT_TOOL_MODE.md`](AGENT_PROMPT_TOOL_MODE.md)

### 決定ログ — 縦ピッチの画角プリセット（2026-08-31 · **実装済**）

**縦サッカーボードでは、局面タブの画角（ズーム）プリセットを横用のまま流用しない。**

| 現状の問題 | 原因 |
|---|---|
| 「全体」以外のプリセットが **横ピッチのゴール方向**（左＝攻撃三方等）前提 | `VIEW_PRESETS` の `cx/cy` は横世界（長さ＝x）で定義 |
| サムネがすべて **横向き** | `ViewportPresetGrid` が `SoccerLandscapeBody` 固定（[`UI_UX.md`](UI_UX.md) §4-5 は横ボード向け） |
| 縦フルで横長キャンバスに **左右にランオフ緑** | `fitField` の contain は仕様どおり（[`VERTICAL_PITCH.md`](VERTICAL_PITCH.md) §5）。画角バグではない |
| プリセット押下で **意図と違う場所にズーム** | `applyViewPreset` が orientation 非対応で `VIEW_PRESETS[id]` をそのまま適用 |

**座標（縦サッカー · 正本）:** 正規化 0–1 は **その向きのピッチ矩形**。縦では **x＝タッチライン幅 · y＝ゴール↔ゴール（長さ）**（`drawBoard` `worldToPitch` / [`VERTICAL_PITCH.md`](VERTICAL_PITCH.md) §3）。

**縦サッカーで UI に出すプリセット（3 種のみ）:**

| id | ラベル | 意味 |
|---|---|---|
| `full` | 全体 | フルピッチ |
| `final-third-left` | FT上 | 上ゴール側ファイナルサード |
| `final-third-right` | FT下 | 下ゴール側ファイナルサード |

**縦では出さない:** ペナ · CK · スロー — 正方形カメラではペナと FT がほぼ同枠になり差が出ない。ペナ寄りは FT + 手動ズーム。セットプレーは局面複製で対応。

**やる（Phase 1 · サッカー縦のみ）:**

1. **orientation 対応のプリセット解決** — `applyViewPreset` / `viewportMatchesPreset` が `pitchOrientation` を見る。横は既存 `VIEW_PRESETS` 不変
2. **縦用プリセット表** — 上記 3 種の `cx/cy` を縦世界で定義（`PORTRAIT_SOCCER_VIEW_PRESETS`）
3. **`ViewportPresetGrid`** — `pitchOrientation === "portrait"` かつ soccer のとき `SoccerPortraitBody` + 縦 viewBox（46×72）のファインダー。ラベルは FT上/下 · Short + `title`
4. **向き切替時** — 既存どおり `resetAllScenesForOrientationChange` で `DEFAULT_VIEWPORT`（維持）。横局面の viewport を縦に持ち込まない
5. **回帰** — 横ボード・バスケ・配信 16:9 contain は変えない

**Later（本プロンプト外）:** 縦の `hideHalf`（left/right ラベルが空間とズレる）· Export 画角プリセットの縦対応 · 編集窓いっぱいに自動ズーム（contain 方針と要議論）

実装プロンプト: [`AGENT_PROMPT_PORTRAIT_VIEWPORT.md`](AGENT_PROMPT_PORTRAIT_VIEWPORT.md)

### 決定ログ — 局面ごとの画角（2026-08-25）

**画角の正本は `scene.viewport`。** 局面＝配置＋描画＋カメラの再現パッケージ。CK 局面に切り替えると CK フレームに戻る。

- **無料:** 組み込みプリセット・局面記憶・Export current
- **Pro:** `viewportTemplates`（名前付き・試合横断）。UI は `FEATURE_PRO_VIEWPORT_TEMPLATES=false` まで非表示
- **`board.viewport`:** 移行用のみ（旧データを各局面へコピー）

### 決定ログ — 画角プリセットはキャプチャ再現単位（2026-08-25）

**プリセット画角はホイールのショートカットではなく、解説キャプチャの再現フレーム。**

CK 角の旧値はコーナー弧だけ入りゴールが外れ、サッカーライターが CK 戦術を語れなかった。毎回の微調整はプロダクト失敗。

- **原則:** ランドマーク必須セット → AABB → zoom/cx/cy（点ズーム禁止）
- **CK Must（動きの語彙）:** 角・ゴール・ペナ・ショート際・**レストDF（ハーフ付近）**。ゴール入りだけでは不足（教本・配信図）
- **外側:** ゴール裏・ピッチサイドは規定余白のみ（`CAPTURE_OUTSIDE_MARGIN`）。余りはピッチ内。毎回同じ余白＝ブランド再現
- **正本:** [`VIEWPORT_RESEARCH.md`](VIEWPORT_RESEARCH.md)。CK は `viewportCoveringRect`
- **参考:** [Professionalsoccercoaching](https://ja.professionalsoccercoaching.com/corners/soccercornerplays2) · [DiBernardo](https://coachdibernardo.com/2014/10/22/corner-kick-basics-attacking-defensive-corner-standard-set-up/) · [NYT](https://www.nytimes.com/interactive/2022/12/05/sports/world-cup/corner-kicks-world-cup.html)
### 決定ログ — ホーム／アウェイのデータ分割（2026-08-25）

**判断: 局面の配置は分けない。名簿は既に分けている。ライブ Scene を `homePieces` / `awayPieces` に割らない。**

| 層 | 持ち方 | 理由 |
|----|--------|------|
| **身分（名簿）** | `roster.home` / `roster.away`（既に別データセット） | 背番号・名前・キットはチーム所有。正しい |
| **局面の配置** | `scene.pieces[]`＋`team`（同一座標空間） | 「両軍シミュレーション」は相対位置・線・ボールが共面必須 |
| **見せ方** | `teamFocus`（ビュー） | データ分割ではなく投影 |

**分けたくなる動機（ホーム形→アウェイ形→重ね）は本物。** ただしそれは **編集セッション／表示の話** であり、永続モデルを二平面にすると次が壊れる。

- 両軍表示時のヒット・重なり・パス／プレス線（跨チーム）
- ボール1個・描画オブジェクト1レイヤの所有権
- Undo・選択・Export の「いまの局面」
- フィルタで隠した側への誤編集より、**マージ／同期バグの方が高い**

**Later（有料 Local Library と整合）:** チーム単位の **配置スナップショット**（ホーム形を保存して局面へ適用）は別データセットにしてよい。それは Scene のライブ状態の分割ではなく、テンプレ／ライブラリ。運営カタログではない。クラウド正本化ではない（§6）。

- **やる:** 身分はチーム別。局面は共面 `pieces[]`。表示は `teamFocus`
- **やらない:** `Scene.homePieces` / `awayPieces` へのスキーマ分割（いまのジョブのため）

### 決定ログ — 広告（2026-08-24）

**ピッチに広告は絶対に入れない。配信モード（Broadcast）にも入れない。**

配信キャプチャに乗る面（コート・駒・帯・ユーザーロゴ）へ、ZoneBoard 側の広告も、利用者が設定する広告枠も、一切出さない。LP や課金ページの広告・計測は別面。配信中のボードは広告面にしない。

---

## 4. ブランド・ドメイン

### 4-1. SUGUDASU / SUGUDASU Sports は不採用

すぐだす（3分実務）と長時間作業机は逆。ペルソナも別。表向きに SUGUDASU を付けない。

### 4-2. ドメイン決定ログ

| 候補 | 結果 |
|------|------|
| pitch* / field* | 屋外バイアス → 却下 |
| koma* | 海外で弱い → 却下 |
| FormaBoard / PieceBoard `.com` | 取得不可 |
| **zoneboard.com** | **プレミアム約64万 → 不取得** |
| zone-board.com（ハイフン） | 本拠にしない（口頭・入力で負ける） |
| **zoneboard.app** | **本拠**（通常価格帯・お名前.comで取得手続き） |

一覧の「○・0円」を信じず、**カート金額を見てから支払う**こと（`.com` で一度罠にかかった）。

### 4-3. 海外展開

正本の調査と決定: [`LOCALE_MARKETS.md`](LOCALE_MARKETS.md)（2026-08-24）。

- **1ドメイン**（`zoneboard.app`）。DNS 国別振り分けは初期やらない
- **公開デフォルトは英語**（PH / 欧州戦術・OBS の共通語）。同じ URL に JA/FR/DE を積まない
- キャンバス（駒名・クラブ）は言語非依存。chrome / LP / 法務だけがロケール
- 次の UI 面は **`/es/` → `/pt/` → `/pl/` → `/fr/`**。`/ja/` は JP ローンチ用にソースへ残す。**pl は en 正本**（[`AGENT_PROMPT_I18N_PL.md`](AGENT_PROMPT_I18N_PL.md)）
- 5大リーグはキット・フォーメの話。UI を英西伊独仏で揃える理由にはしない
- 決済・税・価格は言語面と別チケット

### 4-4. お名前.com 取得時の注意

- レンタルサーバー・「今すぐ利用する」は **利用しない**
- 「すでにサーバーをお持ちの方」（他社・Cloudflare予定）を選ぶ
- **ドメインだけ**買う

---

## 5. 競合・差別化

| 既存 | 差分 |
|------|------|
| Googleスライド | 最初から戦術ボード |
| TacticalPad 等 | 配信モードでキャンバス最大化 |
| TacticsLista 等 | ピッチ大 + **自分のロゴ透かし**（運営ロゴではない） |

横並びの機能表・LP 用の短い言い方・Export 比率ズーム判定: [`COMPETITIVE_LP.md`](COMPETITIVE_LP.md)（2026-08-26）。LP 本体に比較表は載せない（[`LP_STRUCTURE.md`](LP_STRUCTURE.md)）。

### 決定ログ — Export 画角に比率連動ズームは付けない（2026-08-26）

**4:5 / Story / Square は媒体キャンバスの形。切り替えても viewport ズームは変えない。**

媒体（IG Feed・Stories/Reels・X/Threads）が欲しいのは画面占有の比率であり、カメラ距離ではない。寄る／引くはホイール・投稿フォーカス・局面画角。パンは PNG 枠ドラッグ。四役とも No。詳細は [`COMPETITIVE_LP.md`](COMPETITIVE_LP.md) §0 · [`SOCIAL_OUTPUT.md`](SOCIAL_OUTPUT.md)。

---

## 6. 収益メモ

長時間セッション → Privnote型よりマシ。SUGUDASU本線とは別勘定。初期は無料+広告 or 後から Pro を検討可。

### 決定ログ — Pro = Local Library Entitlement（2026-08-27）

**クラウド盤面 SaaS を Pro の定義にしない。** Deep Research（競合境界・OBS 机・GDPR／運用コスト）と CTO/CMO/CPO 合議で確定。

> Pro は「クラウドの机」ではなく **「端末の引き出し」** を売る。  
> 描画・Broadcast・ロゴ・今夜の名簿貼り付けは無料のまま。アカウント必須にしない。

| 判定 | 内容 |
|------|------|
| **採用（Path 1）** | Local Library Pro — 名前付き選手セット、名前付き画角テンプレ、ボード／局面上限の緩和、JSON import/export |
| **棄却** | 盤面全体のクラウド正本化・ログイン必須を Pro の核にすること。**ZoneBoard 自前の Online Storage（預かりクラウド）** |
| **マルチデバイス** | **BYO Storage** — ユーザが既に持つ Drive / Dropbox / iCloud 等へ JSON を置く／開く。ZB は保管庫を運営しない |
| **Later** | Drive picker 等の深い統合。自前土管＋非送信認証（下記）。手運び JSON で足りない需要がインタビューで見えてから |
| **価格** | 未公開。一回買い（仮 $29–$39）または低額年額は仮説。配信者インタビュー後に決める。月額サブスクを本線にしない |
| **コピー** | Squad Presets / Matchday Viewports / JSON Export。使わない: Season roster management / Cloud sync（自社） / Drill templates |

**常に無料（楔）:** 自由描画・駒操作、Broadcast、ユーザーロゴ、単試合の手入力／貼り付け名簿。

**実装の足場:** [`src/lib/plan.ts`](../src/lib/plan.ts)（`activePlan` / entitlements）。Paywall UI・Merchant of Record は未着手。

**JSON backup:** ブラウザキャッシュ消失リスクへの逃げ道。export は楔として **無料でも出せる方向**を優先し、Pro は「複数ライブラリに取り込む／名前付きセットを保有する」側に寄せる（細部は実装時）。

#### Library 一覧 UI（2026-08-27）

Pro の「引き出し」には **一覧面が必要**（探す・改名・削除・適用・書き出し）。ただし第二ホーム／ダッシュボードにはしない。

| 判定 | 内容 |
|------|------|
| **置く場所** | Prep ドロワー内のタブ（または同等の準備面）「Library」 |
| **中身** | Squad Presets 一覧 + Matchday Viewports 一覧（適用＝このボード／この局面へ） |
| **やらない** | `/library` を第一画面にする、トップバー常設、クラウドの「最近のプロジェクト」ブラウザ |
| **空状態** | 「今夜の名簿からセットを保存」への導線。無料はセット無しでも今夜の試合は可 |

#### 一覧ラベル・正本・競合軸（2026-08-27）

> 保存一覧の並び／視認ラベル／ファイル命名で差を取らない。競合比較の俎上に載せない。

**競合の保存リストはコモディティ:** タイトル（ユーザ命名）+ 更新日（+ あればサムネ／フォルダ）。TACTICALista・軽量 Web ボード・TacticalPad/Hudl・スライド代用品いずれも同型。「日付 · 大会 · A vs B」は配信者が自分でそう名付けると見やすい、という衛生であって、競合ができない軸ではない。

**差は引き続き机の契約:** Broadcast、ロゴ主権、OBS に載るピッチ、登録なし楔、Pro = Local Library の中身（名簿・画角の再利用）。一覧 UX・命名規約・フォルダ走査で勝たない。

**自社実装用の視認キー（差別化ではない・迷わないための内部定義）:**

| 種別 | 主表示 | 備考 |
|------|--------|------|
| 試合ボード（机） | `matchLabel` → home vs away → `title`（既存 `boardDisplayName`） | 「いつ ·（大会）· 誰対誰」はユーザがラベルに書く。`competition` 専用カラムは Later |
| Squad preset | セット名（例: Arsenal 25/26） | 日付×対戦を必須にしない。机から保存するとき初期名だけ借りてよい |
| Viewport template | 番組用の呼び名（例: CK wide） | 同上 |

**正本:** v1 は **アプリ内（local entitlement）**。ファイルは持ち運び封筒（`kind` + 表示用 title / matchLabel）。一覧はアプリ状態を出す。

**任意フォルダ読み（Directory picker / File System Access）:** Later。フォルダを Library 正本にすると形式・命名・壊れた JSON・ブラウザ差（Chrome強い／Safari弱い）が一気に本線化する。v1 では採らない。Drive OAuth 深い統合も Later（既存 BYO 段階表どおり）。

#### 同期・BYO Storage（2026-08-27）

> みんな既に Online Storage を持っている。ZB がもう一つ持つ必要はない。**参照する（指す）**。

| 段階 | 内容 |
|------|------|
| **今〜Pro v1** | 手動 JSON の書き出し／読み込み（BYO の最小形）。ノート↔配信 PC・共ホスト共有はファイル手渡し |
| **Later** | 「ユーザの Drive 等に保存／開く」（OAuth は **ストレージ提供者のログイン**であり、ZB ユーザ ID ではない、と文書で分離） |
| **本線にしない** | スマホでライブ駒操作を前提にした三画面リアルタイム同期（watchalong 本線は固定 PC + OBS） |

PC / Tablet / SP の「汎用」は **同じファイル形式が開けること**で達成する。ZB 同期サーバで達成しない。

#### 認証・ユーザ ID・1Password 式（2026-08-27）

参照: [SUGUDASU「非送信」× Sync（Zenn）](https://zenn.dev/sugudasu/articles/abe874966cf373) — 憲法は SUGUDASU と揃えてよい。

| レイヤ | 方針 |
|--------|------|
| **Pro（Local Library）** | ログイン不要。ユーザ ID を持つ場面を作らない |
| **課金証明** | ライセンスキー等（MoR）。メール名簿型の自前アカウントは採らない |
| **自前 Sync 土管が要る場合（Later・非本線）** | 通常の「メール＋パスワードをサーバへ」は採らない。Secret Key + パスワード非送出（1Password / SRP 系）を検討。サーバは ciphertext の土管 |
| **ZB での優先** | BYO で足りる間は自前 Sync を作らない。SUGUDASU Sync と設計を共有できる予約席に留める |

#### VC・マネタイズの見せ方（2026-08-27）

ストレージ課金・預かりクラウドの単位経済には振り切らない。物語は次の三段:

```
Free wedge     描画 / Broadcast / ロゴ     → 成長・口コミ
Pro (ARPU)     Local Library               → 本命の収益（準備時間の削減）
Portability    BYO file（ユーザの保管庫）   → 預からない・マルチデバイス
```

Gross margin は静的配信 + MoR 前提で高く語れる。転換率・価格はインタビュー前は仮説のまま。

### 無料 / Pro（方針・未実装）

| | **無料** | **Pro** |
|--|----------|---------|
| コア（配信・描画・ロゴ・ズーム） | 全部使える | 同じ（楔は削らない） |
| 試合ボード / 局面 | 上限あり（3 / 8） | 緩和（`PLAN_LIMITS.pro`） |
| **選手セット** | いまの試合ボードに紐づく名簿のみ | **名前付きセットを複数保有（ローカル）** |
| **画角** | 組み込みプリセット・**局面ごとの記憶**（`scene.viewport`）・Export＝いまの画角 | **名前付き画角テンプレ**（試合をまたぐローカルライブラリ） |
| **持ち運び** | JSON export（逃げ道・共有・BYO の種） | import → ライブラリ反映、上限緩和。保管庫はユーザ側（BYO） |

**画角の無料 / Pro 切り分け（2026-08-25）:**

| | **無料** | **Pro** |
|--|----------|---------|
| CK 等の組み込みプリセット | ○ | 同じ |
| 局面切替で画角が復元（`scene.viewport`） | ○ | 同じ |
| Export「いまのズーム」＝画面と一致 | ○ | 同じ |
| Export **Post framing**（Full / FT左 / FT右） | ○（手動ズームで代替可） | **ショートカット＋将来は保存テンプレから Export** |
| 「うちの番組用」名前付き画角 | — | **複数保存・横断呼び出し（ローカル）** |

組み込みフレームと局面記憶は **キャプチャ再現の楔**。カスタムテンプレは名簿セットと同じ **横断ライブラリ** が Pro 核。実装: `viewportTemplates` + `FEATURE_PRO_VIEWPORT_TEMPLATES`（`hasEntitlement("viewportTemplates")`）。

**選手セット（Pro の核）— 役割分担:**

| 誰がやる | 何を |
|----------|------|
| **プロダクト** | カラム（スキーマ）を先に固定。空欄可。無料の試合名簿も同じ形 |
| **ユーザ** | 中身の作成（貼り付け・手入力・ボードから「セットとして保存」） |
| **公式データ** | ライブ取込・スクレイピングは **しない**。必要なら静的な参考テンプレ（番号枠だけ、コピペしやすい表）程度 |

#### チームセット作成 = 貼り付けで完了（2026-08-27）

> どこか（公式サイト・Wikipedia・スプレッドシート・チャット）からコピーした表／行を貼って、セットができる。そこでジョブ終了。

| 判定 | 内容 |
|------|------|
| **本線** | 貼り付け（TSV/CSV/行テキスト）→ スキーマ列にマッピング → 名前を付けて Library に残す |
| **補助** | 手直し・手入力・今夜のボード名簿から「セットとして保存」 |
| **やらない** | 外部 DB 連携、Transfermarkt 等のスクレイプ、ログインして名簿を取るコネクタ、多段ウィザード |
| **プロダクトの仕事** | 貼りやすい列（`number` 必須、他は空欄可）とパースの寛容さ。データの正確性はユーザ側 |

**現行の実運用（内部・2026-08-27）:** PL 公式 → スプシで整える → ZB に貼る。コネクタ無しで回っている。セット UI もこの導線（コピー元は公式に限らない）を本線にする。

無料の「今夜の名簿」も同じ入口。Pro はそれを **名前付きセットとして残せる**だけ。

| カラム | 必須 | 備考 |
|--------|------|------|
| `number` | **必須** | 識別子 |
| `label` | 任意 | 名前 |
| `preferredFoot` | 任意 | `L` / `R` / `B` |
| `defaultColor` | 任意 | セット単位のチーム色 |

セットの CRUD は Pro 実装時（**ローカル entitlement** + Prep 内 Library 一覧。ZB 預かりクラウドはしない）。v1 はボード紐づき名簿のみ（カラムは既に同じ）。

地方大会は番号だけ、代表戦は番号＋名前＋利き足、をセットごと保存して呼び出せる。駒カードは **トリプルクリック / 右クリックで欲しいときだけ**（単クリックは移動のみ。ダブルクリックは使わない）。カード項目は **背番号・名前・利き足** のみ（国籍・成績・プレースタイル文は載せない）。向き UI・視野角扇は出さない（決定ログ 2026-08-25）。

**価格決定前のインタビュー（EN watchalong）:**

1. 名簿は何日前に／キックオフ何分前に、どのマシンで入れるか  
2. 一回買い（例 $35・ローカルライブラリ）と月額同期アカウントのどちらが欲しいか  
3. 共ホスト／編集者へのボード共有を今どうしているか  

**Later — コミュニティ内のデータ流通:**

カラムが固定されていれば、セットはファイル／リンクでやり取りできる。解説者同士・部活同士で「今季の背番号表」を共有するマーケットがコミュニティ内に自然にできるとよい。運営が選手 DB を売るのではなく、**ユーザが作ったセットの流通**。権利・正確性は投稿者責任。Pro の「複数セット保有」がその前提になる。

**流通の場所（自ドメイン下には置かない）:**

| やること | やらないこと |
|----------|--------------|
| エクスポート形式を公開し、FAQ に「共有のしかた」を書く | `zoneboard.app` 上の公式マーケット／フォーラム |
| 既存コミュニティへの**軽い種まき**（スレッド・ピン留め例） | 運営が FanCommunity をホスト・モデレートする |
| 完全な自然発生だけに賭ける（フォーマットが知られない） | Reddit だけを公式コミュニティにする（日本人には敷居が高い） |

種まきの置き場は「すでに人がいる場所」に寄せる。

- **日本語:** X（解説・部活の既存タイムライン）、Discord（配信者サーバ）、note など。必要なら FAQ から「共有タグ例」を示す程度
- **英語:** Reddit スレッドは可（EN 向け）。JP の主戦場にはしない

FanCommunity はプロダクト機能ではなく、**フォーマットが揃った結果として外で起きるもの**。運営の仕事はスキーマと import/export と FAQ まで。

---

### 決定ログ — 局面メモ（2026-08-28）

**主用途は配信ではなく振り返り。** 解説・指導者が「なぜこう置いたか」を局面と一緒に残す。配信（B）・PNG Export には出さない。

| 段階 | 内容 |
|---|---|
| **v1（済）** | `scene.notes` プレーンテキスト · Scenes ドロワー textarea · localStorage 保存 · チップ hover で先読み |
| **v1.5** | ボード内検索（label + notes）— B-063 |
| **Pro / Library** | 横断検索 · 任意タグ · 芝上メモレイヤーは Later |

ピッチの Text ツールはオンボード用のまま。メモは **局面アーカイブのキャプション** として分離する。

### 決定ログ — 配信 matte・ズーム時の線スケール・配信 chrome（2026-08-28）

**配信の黒背景は 16:9 ガイド。ズーム時の線太さは非対称のまま維持。配信 chrome はタッチ向けに残す。**

#### 黒 matte（`BROADCAST_MATTE`）

| 領域 | 色 | 意味 |
|------|-----|------|
| **フレーム外** | `#0a0a0a` | **16:9 配信フレームの正本**。OBS キャプチャの安全枠兼ガイド。クロマキー用ではない |
| **フレーム内・ピッチ外** | `#1f5230`（芝時） | ランオフ（サブ帯等のバッファ）。編集モードの「窓いっぱい芝」とは別契約 |

- 窓が 16:9 でないとき → 外周に黒 matte。横リサイズで左右が増えるだけで、16:9 島（帯＋ピッチ）は相似のまま
- ズームインでピッチが 16:9 幅を埋めないとき → **フレーム内**にも黒 matte が見える（正常）
- レイアウト詳細: [`BROADCAST_LAYOUT.md`](BROADCAST_LAYOUT.md)

#### ズーム時の描画スケール（2026-08-25 駒密度の延長）

| レイヤー | ズームイン時 | `densityZoom` |
|----------|-------------|---------------|
| 駒・ボール | 画面サイズほぼ一定 | ○ |
| Pen・Link | ほぼ一定 | ○ |
| パス／ラン・ピッチ白線・ゾーン・テキスト | 太く／大きくなる | ×（ピッチ矩形に比例） |

- **意図:** 駒＝「誰」（読みやすさ一定）。線＝「どこ」（ピッチ上の幾何としてスケール）。フル画角で引いた線をズームしても座標は破綻しない
- **判定: 現状維持。** 全レイヤに密度補正をかけない（パス線が CK 寄りで細くなりすぎる）
- **Later（任意）:** パス／ランに弱い補正（例 `/ sqrt(zoom)`）。気になったら BACKLOG に積む。ピッチ白線まで均す必要は薄い
- 実装: `densityZoom` · `pieceRadius` · `penStrokeWidth` · `linkStrokeWidth` in `src/canvas/drawBoard.ts`

#### 配信 chrome（右下・左下）

- **残す:** 局面切替・ツール・解除・試合操作（たたみ可）。PC ホットキーが無い **タブレット／SP** では唯一の導線。初見ユーザを殺さない
- **OBS で芝だけ:** Window Capture の Client area で chrome を外す（運用側）
- 旧表現「配信キャプチャ面にメニューは出ない」は本決定で訂正（[`LOCALE_MARKETS.md`](LOCALE_MARKETS.md) 等）

### 決定ログ — 局面複製の案内と配信中複製（2026-08-28）

**「元に戻す」はリセットボタンではなく、局面複製＋`[` `]` 切替。** 初期配置リセット専用コマンドは v1 で作らない（前項の議論）。

| 判断 | 内容 |
|------|------|
| **案内** | Scenes ヒント・How-to で「動かす前に複製 → 元は `[` `]`」を教える |
| **配信中複製** | 配信 chrome（局面切替の隣）から `addScene` を露出。B 解除不要 |
| **シームレス** | クロスフェード等のアニメは付けない。瞬時切替＋コピー側に留まる（既存 `addScene`） |
| **やらない** | 初期配置リセット · 局面間トランジション · 確認ダイアログ（非破壊） |

- **意図:** ライブで1パターン試して元の図に戻す。About の「duplicate the scene」をボード内で発見可能にする
- **Pro Library の配置スナップショット**（Later）とは別 — 局面内のライブフォークは無料コア
- 実装: `Editor` broadcast chrome · `scenesHint` / `newSceneHint` · `howTo` · B-065

### 決定ログ — 配信コマンド表（印刷・PDF）（2026-08-28）

**配信者向けにキーボード早見表を印刷／PDF 保存できるようにする。** 芝上のチュートリアルは出さない。How-to と同じ正本（`HOW_TO`）から静的ページを生成する。

| 判断 | 内容 |
|------|------|
| **置き場所** | `/materials/shortcut-sheet/`（EN）· `/materials/shortcut-sheet/ja/`（JA） |
| **DL** | 専用 PDF ファイルは配布しない。ブラウザの印刷 → PDF 保存（運用コスト最小） |
| **導線** | Materials ページ · How-to モーダル（`?` / F1）フッター |
| **中身** | セクション見出し＋キー表のみ（`howTo.ts` と同期） |

- 実装: `src/site/shortcutSheet.ts` · `npm run site:pages` · B-066

### 決定ログ — PNG Export 契約（2026-08-28）

**投稿用 PNG はピッチ本体がデフォルト。試合帯・局面タイトルは配信／投稿文側の仕事。**

| レイヤ | 配信（OBS） | PNG Export（既定） |
|--------|-------------|-------------------|
| **上部試合帯** | `showMatchBanner` ON で表示 | **焼かない**（ピッチ面積を確保） |
| **下部タイトル帯** | — | **`bakeCaption` 既定 OFF**（X/IG の投稿文で足す） |
| **透かし・クレジット** | ユーザ設定 | 既定 OFF（従来どおり） |

- Settings 開閉中のプレビューも帯なし（WYSIWYG）。帯は Settings を閉じれば編集・配信で再表示
- 必要な人だけ `bakeCaption` / `bakeCredit` / 透かしを ON
- 実装: `exportPng.ts` · `BoardCanvas` `suppressMatchBanner` · `SettingsModal` · B-067

### 決定ログ — Pro と Export Post framing（2026-08-28）

**Post framing 全体を Pro 化しない。Current zoom と比率プリセット（4:5 / 16:9 等）は無料固定。**

| 判断 | 内容 |
|------|------|
| **無料（楔）** | Export **Current zoom** · 投稿比率プリセット · 枠ドラッグ（`exportFrameRect`） |
| **Pro（時短）** | Settings の **Full / FT左 / FT右**（`exportFocus !== "current"`）— 毎回ホイールで寄せ直さない |
| **Pro（本線・既存）** | **`viewportTemplates`** から Export（「保存した画角: CK右」）。FT ドロップダウンと二重 UI にしない方向 |
| **やらない** | Post framing 全体の Pro 化 · 比率プリセットの Pro 化 · 無料で Current が使えない状態 |

- 無料ユーザは Ctrl+ホイール → Current Export で FT 相当は再現可能。Pro は **準備時間削減**（名簿セット・画角ライブラリと同じ ARPU 文脈）
- ゲート: `hasEntitlement("viewportTemplates")` または dedicated `exportFraming` — **Pro 課金実装時**（B-023 以降）。いまは全員 Full / FT 可
- 参照: [`SOCIAL_OUTPUT.md`](SOCIAL_OUTPUT.md) · 上表「画角の無料 / Pro 切り分け」

---

## 7. 次のアクション

未着手の一覧は [`BACKLOG.md`](BACKLOG.md) を正とする。

- [x] **RTK · CodeGraph 導入**（[`TOKEN_ECONOMY.md`](TOKEN_ECONOMY.md) — Token 経済圏）
- [x] **`docs/SPEC.md` を書く**（実装可能な仕様 — 承認待ち）
- [x] UI/UX 指針とパネル／設定ブラッシュアップ
- [ ] SPEC 承認（未決事項 U1–U7）— B-001
- [ ] `zoneboard.app` 取得完了の確認 — B-002
- [ ] Pro / 選手セット — B-020 以降
